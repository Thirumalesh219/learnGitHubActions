name: Check for array variables
on:
  workflow_dispatch:

jobs:
  printArray:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Print vairables
        run: echo ${{vars.users}}