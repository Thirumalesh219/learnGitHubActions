name: Sample repository variables and secrets

on:
  workflow_dispatch:

jobs:
  WorkFlowForRepo:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{secrets.RepoSecret}}
      - run: echo ${{vars.RepoVariables}}